.common_container#tag_page
  - if @subtitle.is_a? Array
    - name = "#{@subtitle.last}: #{@subtitle.first}"
  - else
    - name = @subtitle
  %h2<
    %i.icon-tags
    = name
  .row
    .span3
      = render 'games/side'
    .span9
      = render 'tags/tags', tags: @tags, with_pagination: true
