- href = search_tags_path
= form_tag href, id: :search_tag, class: %w(validated form-search), method: :get do
  .input-append
    = search_field_tag :q, params[:q], class: %w(search-query span2)
    = submit_tag t('helpers.submit.search_short'), class: %w(btn)
