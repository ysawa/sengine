$('nav.next_comment').before('<%= escape_javascript(render 'comments/comments', comments: @comments, with_pagination: true, pagination_params: { controller: :comments, action: :index, game_id: @game.id }) %>');
$('nav.next_comment:last').remove();
