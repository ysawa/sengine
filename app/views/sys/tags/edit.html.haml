.common_container#tag_page
  %h2= @subtitle
  = render 'form', tag_element: @tag
  - if @tag.persisted?
    %section.actions.form-actions
      = link_to t('actions.show'), sys_tag_path(@tag), class: 'btn'
