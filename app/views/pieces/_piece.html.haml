- unless defined?(decorator)
  - decorator = PieceDecorator.new(piece)
.piece{ id: decorator.id, role: decorator.role, direction: decorator.direction, class: decorator.html_classes }
  = t("piece.roles.#{piece.role}")
