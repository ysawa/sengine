= form_for @feedback do |f|
  %section.field
    = f.text_area :content, rows: 4, class: :required, placeholder: t('please.feedback')
  %section.actions
    = f.submit t('helpers.submit.submit'), 'data-disable-with' => t('actions.submitting')
