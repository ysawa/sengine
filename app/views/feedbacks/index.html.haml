.common_container#feedback_page
  %h2= @subtitle
  = render 'about'
  - if user_signed_in?
    = render 'form', feedback: @feedback
  = render 'feedbacks', feedbacks: @feedbacks
  = paginate @feedbacks
